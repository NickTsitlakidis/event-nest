# mongodb@7.6.0 loads the "os" module with a dynamic import(), which Node's vm
# (used by jest to sandbox test code) only supports behind this flag. Without it
# the driver silently fails to build its handshake metadata and every connection
# to the in-memory replica set is rejected by mongod.
NODE_OPTIONS=--experimental-vm-modules
